# Copie para .env e preencha. O .env NÃO é versionado.

# Projeto GCP usado como "billing" para consultar o BigQuery da Base dos Dados.
# É o ID do seu projeto no Google Cloud (grátis; 1 TB/mês de consulta sem custo).
BILLING_PROJECT_ID=seu-projeto-gcp

name: 'observatorio'
version: '1.0.0'
config-version: 2

profile: 'observatorio'

model-paths: ["models"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  observatorio:
    staging:
      +materialized: view
    marts:
      +materialized: table
